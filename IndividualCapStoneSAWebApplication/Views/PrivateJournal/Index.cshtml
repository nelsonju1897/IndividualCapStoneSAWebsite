@model IEnumerable<IndividualCapStoneSAWebApplication.Models.PrivateJournal>
    <link href="~/Images/bootstrap-cyborg.css" rel="stylesheet" />
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PrivateJournalName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.JournalEntry)
        </th>        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PrivateJournalName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.JournalEntry)
        </td>        
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PrivateJournalId }) |
            
            @Html.ActionLink("Delete", "Delete", new { id=item.PrivateJournalId })
        </td>
    </tr>
}

</table>
